Here’s a **professional `README.md` (Markdown)** you can use for your GitHub repository **[js‑reacp · GitHub](https://github.com/hasibulpolok/js-reacp.git)**:

---

```markdown
# js‑reacp

A simple JavaScript examples repository that demonstrates basic logic for checking even and odd numbers using different approaches.

This repo mainly contains a JavaScript file that shows how to work with functions, arrow functions, switch cases, and user input to determine whether a number is even or odd.

## 🧠 Features

✔ Function version to check even/odd  
✔ Arrow function version  
✔ Switch‑case based logic  
✔ Browser prompt input example

## 🗂 Project Structure

```

js‑reacp/
├── index.js      # JavaScript demonstration code
└── README.md     # Project documentation

````

## 📌 Usage

1. Clone the repository:

   ```bash
   git clone https://github.com/hasibulpolok/js-reacp.git
````

2. Open `index.js` in a browser environment or Node.js to run the scripts.

   * In a browser, the prompt will ask for user input.
   * In Node.js, some prompt lines may not work — you can comment them out.

## 💻 Example Output

```js
// Function
EvenOdd(50)   // Even Number
EvenOdd(55)   // Odd Number

// Arrow
eoresult       // "OddNumber"

// Switch
evenOddSwitch(10)  // "Even number"
evenOddSwitch(7)   // "Odd number"
```

## ✨ Code Snippet

Here’s part of the logic used:

```js
const EvenOdd2 = (num) => {
  return num % 2 === 0 ? "EvenNumber" : "OddNumber";
};

console.log(EvenOdd2(7)); // OddNumber
```



